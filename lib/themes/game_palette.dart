import 'package:flutter/material.dart';

/// A classe [GamePalette] define todas as cores do game.
/// A paleta de cores foi retirada diretamente do asset pack Ninja Adventure, disponível na itch.io.

class GamePalette {
  static const Color primary = Color(0xFFFF7E4A);
  static const Color onPrimary = Color(0xFFF2EAF1);
  static const Color borderPrimary = Color(0xFFFF3E24);

  static const Color secondary = Color(0xFF141B1B);
  static const Color onSecondary = Color(0xFFF2EAF1);
  static const Color borderSecondary = Color(0xFF819380);

  static const Color primaryShadow = Color(0xFF34322B);
  static const Color secondaryShadow = Color(0xFF85AFBC);

  static const Color error = Color(0xFFE0394C);
  static const Color onError = Color(0xFF020202);

  static const Color background = Color(0xFF686557);
  static const Color onBackground = Color(0xFF020202);

  static const Color textPrimary = Color(0xFFF2EAF1);
  static const Color textSecondary = Color(0xFF020202);
  static const Color boxMenssage = Color(0xFFf2EAF1);
  static const Color borderBoxMenssage = Color(0xFFEA7C42);

  static const Color surface = Color(0xFF141B1B);
  static const Color onSurface = Color(0xFFF2EAF1);
}
